docker.enabled = true

manifest {
    name                                  = 'exome-vntr-nf'
    version                               = '0.4.2'
    description                           = 'Nextflow pipeline to analyse dark regions'
    author                                = 'Sebastian Schönherr'
    homePage                              = 'https://github.com/seppinho/exome-cnv-nf'
    mainScript                            = 'main.nf'
    nextflowVersion                       = '!>=21.04.0'
}

// Global default params, used in configs
params {

    //Required inputs
    project                        = null
    input                          = null
    gold                           = null
    region                         = null
    build                          = null
    region                         = null
    reference                      = null
    signature_split_value          = 64
    mutserve_detection_limit       = 0.01
    mutserve_baseQ                 = 30
    mutserve_mapQ                  = 20
    mutserve_alignQ                = 30
    contig                         = null
    threads                        = (Runtime.runtime.availableProcessors() - 1)
    //optional
    outdir                         = null

}

process.container                 = 'quay.io/genepi/exome-vntr-nf:v0.4.1'

profiles {
    debug { process.beforeScript          = 'echo $HOSTNAME' }

    docker {
        docker.enabled                    = true
        docker.userEmulation              = true
        singularity.enabled               = false
    }

    singularity {
        singularity.enabled               = true
        singularity.autoMounts            = true
        docker.enabled                    = false
    }

    development {
        process.container                 = 'genepi/exome-vntr-nf:latest'
        docker.enabled                    = true
        docker.userEmulation              = true
        resume                            = true
        singularity.enabled               = false
    }

   test      {
        includeConfig 'conf/test.config'
   }

}
